# 用户配置文件
# 复制此文件为 config/config/group-insight.yaml 并填入你的配置

groupManager:
  ai:
    # 在此填入你的 AI API Key
    apiKey: 'your-api-key-here'

    # 如果使用国内镜像或自定义端点，可以配置 baseURL
    # baseURL: 'https://your-api-endpoint.com'

  # 可以覆盖默认配置
  # retentionDays: 14
  # wordCloud:
  #   maxWords: 150
